server:
  port: 8081

spring:
  application:
    name: ms.login.screm
datasource:
  url: $DB_URL
  username: $DB_USERNAME
  password: $DB_PASSWORD
  jpa:
    hibernate:
      ddl-auto: update
api:
  security:
    token:
      secret-password: ${JWT_TOKEN_SECRET_PASSWORD}}

